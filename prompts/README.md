# Prompt templates

Hand-authored example content remains under `content/`. Reusable agent workflows
belong under `.agents/skills/`; deterministic ingestion, refinement, practice,
validation, and release behavior belongs under `tools/`. Keep this directory
for future non-skill prompt assets only when they cannot be expressed through
those focused workflows.
